// Tailwind v4 CSS processing is handled by the @tailwindcss/vite plugin in vite.config.js.
// PostCSS is only needed for autoprefixer when NOT using the Vite plugin.
export default {
  plugins: {},
}
